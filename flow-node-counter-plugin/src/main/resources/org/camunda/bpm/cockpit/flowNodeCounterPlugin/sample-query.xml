<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cockpit.sample">

  <resultMap id="flowNodeCountMap" type="org.camunda.bpm.cockpit.flowNodeCounterPlugin.db.FlowNodeCountDto">
    <result property="month" column="MONTH_" jdbcType="VARCHAR" />
    <result property="year" column="YEAR_" jdbcType="VARCHAR"/>
    <result property="flowNodeCount" column="FLOWNODECOUNT_" jdbcType="INTEGER" />
  </resultMap>

  <select id="selectFlowNodeCounterPerMonth" resultMap="flowNodeCountMap">
SELECT top 12 monthname(start_time_) as month_, year(start_time_) as year_, count(start_time_) as flowNodeCount_
FROM ACT_HI_ACTINST 
group by year(start_time_), month(start_time_), monthname(start_time_)
order by year(start_time_), month(start_time_) desc
  </select>

</mapper>